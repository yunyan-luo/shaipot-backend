{
  "scripts": {
    "clean": "node-gyp clean",
    "configure": "node-gyp configure",
    "build": "node-gyp build",
    "start": "npm run clean && npm run configure && npm run build && node server.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "bn.js": "^5.2.1",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.1",
    "mongodb": "^6.9.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "node-addon-api": "^8.1.0",
    "readline": "^1.3.0"
  }
}
